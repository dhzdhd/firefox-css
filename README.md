# Custom Firefox CSS

## Usage

- Clone the repo to a new `chrome` folder in the default profile (found by searching about:profiles)
- Install [sass](https://sass-lang.com/install/)
- Compile the main `src/userChrome.sass` file - `sass src/userChrome.sass userChrome.css`
- Enable the necessary options in Firefox and reload the browser
